query FetchTrendPosts($limit: Int!, $offset: Int!) {
  posts(
    limit: $limit
    offset: $offset
    order_by: { likes_aggregate: { count: desc } }
  ) {
    ...PostCard
  }
  postsAggregate {
    aggregate {
      count
    }
  }
}
