query FetchPost($postId: Int!, $userId: String!, $isLoggedIn: Boolean = false) {
  post(id: $postId) {
    id
    title
    description
    content
    createdAt
    updatedAt
    user {
      id
      name
      picture
    }
    likes_aggregate {
      aggregate {
        count
      }
    }
    userLikesAggregate: likes_aggregate(where: { userId: { _eq: $userId } })
      @include(if: $isLoggedIn) {
      aggregate {
        count
      }
    }
    # NOTE: パーミッションの設定で自分のストックしか取得できない
    stocks @include(if: $isLoggedIn) {
      id
    }
    ...PostTags
  }
}
