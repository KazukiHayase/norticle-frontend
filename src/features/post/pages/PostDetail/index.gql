query FetchPost($postId: Int!) {
  post(id: $postId) {
    id
    title
    description
    content
    createdAt
    updatedAt
    user {
      id
      name
      picture
    }
    taggings {
      id
      tag {
        id
        name
      }
    }
  }
}
